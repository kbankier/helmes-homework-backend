spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: admin
    password: really_secure_password
  flyway:
    enabled: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
database:
  fresh-schema-on-start: true
auth:
  username: frontend_user
  password: really_secure_password
